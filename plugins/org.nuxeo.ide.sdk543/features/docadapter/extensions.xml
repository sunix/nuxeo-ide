<component name="${package}.${className}">

   <extension target="org.nuxeo.ecm.core.api.DocumentAdapterService" point="adapters">
       <adapter
           class="${package}.${className}"
           factory="${package}.${className}Factory"/>
   </extension>
    
</component>
